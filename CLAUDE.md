# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## 코딩 행동 지침 (Karpathy Guidelines)

**요약:** 속도보다 신중함. 단순한 작업엔 판단력 사용.

### 1. 코딩 전 생각하기

**가정하지 않기. 혼란 숨기지 않기. 트레이드오프 드러내기.**

구현 전:
- 가정이 있으면 명시적으로 말하기. 불확실하면 질문하기.
- 여러 해석이 가능하면 제시하기 — 조용히 하나만 고르지 않기.
- 더 단순한 방법이 있으면 말하기. 필요하면 반론 제시.
- 불명확한 게 있으면 멈추기. 뭐가 헷갈리는지 명시하고 질문하기.

### 2. 단순함 우선

**문제를 푸는 최소한의 코드. 추측성 코드 금지.**

- 요청받은 것 이상의 기능 추가 금지.
- 단일 사용 코드에 추상화 금지.
- 요청하지 않은 "유연성"이나 "확장성" 금지.
- 불가능한 시나리오 에러 핸들링 금지.
- 200줄이 50줄로 될 수 있으면 다시 쓰기.

자문: "시니어 개발자가 이걸 보면 과하다고 할까?" → 그렇다면 단순화.

### 3. 외과적 변경

**꼭 필요한 것만 건드리기. 내가 만든 mess만 정리하기.**

기존 코드 편집 시:
- 인접한 코드, 주석, 포맷 "개선" 금지.
- 멀쩡한 것 리팩토링 금지.
- 내 스타일이 달라도 기존 스타일 맞추기.
- 관계없는 죽은 코드를 발견하면 — 언급만 하고 삭제 금지.

내 변경이 고아를 만들었을 때:
- 내 변경으로 쓰이지 않게 된 import/변수/함수는 제거.
- 기존에 있던 죽은 코드는 요청 없이 제거 금지.

기준: 변경된 모든 줄이 사용자의 요청으로 직접 이어져야 한다.

### 4. 목표 중심 실행

**성공 기준 정의. 확인될 때까지 반복.**

작업을 검증 가능한 목표로 변환:
- "검증 추가" → "잘못된 입력 테스트 작성 후 통과시키기"
- "버그 수정" → "버그 재현 테스트 작성 후 통과시키기"

여러 단계 작업엔 간단한 계획 먼저:
```
1. [단계] → 확인: [체크]
2. [단계] → 확인: [체크]
3. [단계] → 확인: [체크]
```


---

## ⚠️ Migration Note (bunker-bloom 브랜치)

이 문서는 **Debt Farm 컨셉** 기준으로 작성됨. `bunker-bloom` 브랜치에서는 새 컨셉(턴제 벙커 생존 시뮬레이션)으로 전환 중이며, 현재 유효한 코드는 다음만:

- `_Project/Scripts/BunkerBloom/**` — 새 도메인 코드
- `_Project/Scripts/Core/AudioManager.cs`, `SoundManager.cs`, `BGMManager.cs`
- `_Project/Scripts/UI/TimingBarUI.cs`, `TextTypewriter.cs`
- `_Project/Scripts/Juice/ActionFeedback.cs`
- `Editor/TimingBarUIEditor.cs`

위 외 시스템 설명(농사·빚·정산·트렌드·드론샵·생존자 등)은 모두 삭제됨.  
이 문서의 내용은 **BunkerBloom 시스템이 안정화된 후 전체 재작성 예정**.
